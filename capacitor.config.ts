import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.blackshark.github',
  appName: '6502',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
